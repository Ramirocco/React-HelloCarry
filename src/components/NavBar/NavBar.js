import { CardWidget } from "../CartWidget/CartWidget";

export const NavBar = () => {
    return (
        <div>
            <nav>
                <CardWidget/>
            </nav>
        </div>


    );
}
